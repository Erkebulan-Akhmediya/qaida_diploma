import 'package:flutter/material.dart';

class Authorized extends StatelessWidget {
  const Authorized({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Авторизованный профиль'),
    );
  }
}